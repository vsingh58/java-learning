package org.apache.hadoop.examples.offgrid_sort;

import org.testng.Assert;
import org.testng.annotations.Test;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

/**
 * Created with IntelliJ IDEA.
 * User: zhishan
 * Date: 1/2/14
 * Time: 9:57 PM
 * To change this template use File | Settings | File Templates.
 */
public class BubbleSortTest {
/*
    @Test(description = "Double type")
    public void testBubbleSort() throws Exception {
        Double [] doubles = {2.34, 1.0,0.344,2.54,0.46,3.2013, };
        BubbleSort.<Double>bubbleSort(doubles);
        Arrays.<Double>sort(doubles);
        Double [] expects = {0.344,0.46, 1.0, 2.34, 2.54,3.2013};
        Assert.assertEquals(doubles, expects, "Double type sort fails");
    }
    @Test(description = "String type")
    public void testBubbleSort_String() throws Exception {
        List<String> strList = new ArrayList<>();
        strList.add("hello");
        strList.add("world");
        strList.add("cdfg");
        strList.add("Cdfg");
        strList.add("cDfg");
        String [] actual = new String[strList.size()];
        strList.<String>toArray(actual);
        BubbleSort.<String>bubbleSort(actual);
        //String[])strList.toArray(),
        // java.lang.ClassCastException: [Ljava.lang.Object; cannot be cast to [Ljava.lang.String;

        String [] expects = {"Cdfg", "cDfg", "cdfg","hello", "world"};
        Assert.assertEquals( actual, expects, "String type sort fails");
    }


    @Test(description = "Double type")
    public void testBubbleSort_V2() throws Exception {
        Double [] doubles = {2.34, 1.0,0.344,2.54,0.46,3.2013, };
        BubbleSort.<Double>bubbleSort(doubles);
        Arrays.<Double>sort(doubles);
        Double [] expects = {0.344,0.46, 1.0, 2.34, 2.54,3.2013};
        Assert.assertEquals(doubles, expects, "Double type sort fails");
    }
    @Test(description = "String type")
    public void testBubbleSort_String_V2() throws Exception {
        List<String> strList = new ArrayList<>();
        strList.add("hello");
        strList.add("world");
        strList.add("cdfg");
        strList.add("Cdfg");
        strList.add("cDfg");
        String [] actual = new String[strList.size()];
        strList.<String>toArray(actual);
        BubbleSort.<String>bubbleSort(actual);
        //String[])strList.toArray(),
        // java.lang.ClassCastException: [Ljava.lang.Object; cannot be cast to [Ljava.lang.String;

        String [] expects = {"Cdfg", "cDfg", "cdfg","hello", "world"};
        Assert.assertEquals( actual, expects, "String type sort fails");
    }*/
}
